The file timeprofile.dat is generated by running:

./timeprofile.x 42.x -J

The program was run on an Intel Xeon X5670 server, which has
12 cores and 40 GB RAM. The datafile was renames as timeprofileXeon.dat

In addition, the program was run on a MacBook Pro (16-inch, 2021).
The datafile was renames as timeprofileMacOSX.dat


The data obtained in both servers was used to render Fig. 13 of the
article (DensToolKitv2).

